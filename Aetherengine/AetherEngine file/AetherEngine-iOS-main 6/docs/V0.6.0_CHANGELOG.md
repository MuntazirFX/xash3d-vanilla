# AetherEngine v0.6.0 — MDL Skeletal Pose

- Corrected GoldSrc `mstudioseqdesc_t` field offsets used by the sequence parser.
- Added `AetherMDLPose` for compressed animation-channel sampling, quaternion interpolation, bone hierarchy transforms, and CPU skinning.
- Corrected MDL studio model/mesh disk layouts in geometry extraction.
- Retained MDL sequence data in the iOS bridge so animations can advance at runtime.
- Added skinned vertex/normal bridge accessors and per-frame Metal buffer update.
- Added regression coverage for sequence offsets, animation sampling, and hierarchical bone transforms.

## Verification

Host CMake build and CTest: 8/8 tests pass.
